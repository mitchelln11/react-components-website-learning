import checkProdId from './checkProdIds'
import { eCommValues } from '../ecomm/Checklist';

let cartItems = [];

// export function matchIdWithProduct()

// 1. Click button
// 2. Find productId that's equal to the param
// 3. Add product to cartItems array
// 3. b. Minimum add img, title, price, and sale price if applicable

// Need a function to create an object and add to cartItems array ^