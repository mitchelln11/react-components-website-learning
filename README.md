First clone down repo locally. If you haven't already, install node.

To start server, use `npm start`

Running Tests:

Run Single Test File:
`npx jest {{test file name here with extension}} --coverage false --silent false`